# link raw data
ln -s /storage/group/jvh6349/default/Fraxinus/FRAAME_LandscapeGenomics_RADseq/DemultiplexedReads/* ./

# find pop_code with numbers 1 or 2 at the end
find . -maxdepth 1 -name "*[12]_*.fq.gz"
# rename 
for f in *[12]_*.f*q.gz; do mv -n "$f" "$(echo $f | sed 's/[12]_/_/')"; done

#check file numbers after rename
ls -1 ./ | wc -l
